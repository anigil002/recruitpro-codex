# RecruitPro Local Single-User Configuration
# =============================================================================
# This is a ready-to-use configuration for running RecruitPro locally on your
# computer with one user. Simply run: cp .env.local .env
# =============================================================================

# Core Application Settings
# -----------------------------------------------------------------------------
# Change this to a random secret key for your local instance
RECRUITPRO_SECRET_KEY=change-me-dev-secret

# Database Configuration
# -----------------------------------------------------------------------------
# SQLite is perfect for local single-user deployment
RECRUITPRO_DATABASE_URL=sqlite:///./data/recruitpro.db

# CORS Configuration
# -----------------------------------------------------------------------------
RECRUITPRO_CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# Background Queue (Redis + RQ) - OPTIONAL for local use
# -----------------------------------------------------------------------------
# For local single-user deployment, Redis is optional. If not configured,
# background tasks will run synchronously (which is fine for single-user use).
# To enable Redis, uncomment the line below and ensure Redis is running:
# RECRUITPRO_REDIS_URL=redis://localhost:6379/0

# Rate Limiting - OPTIONAL for local use
# -----------------------------------------------------------------------------
# For local single-user deployment, rate limiting can be disabled
RECRUITPRO_RATE_LIMIT_ENABLED=false

# Security Settings
# -----------------------------------------------------------------------------
# HTTPS not required for local development
RECRUITPRO_FORCE_HTTPS=false
# Password history check (number of previous passwords to check)
RECRUITPRO_PASSWORD_HISTORY_COUNT=5

# Storage Path (relative to project root)
# -----------------------------------------------------------------------------
RECRUITPRO_STORAGE_PATH=storage

# AI Integration (Google Gemini) - OPTIONAL
# -----------------------------------------------------------------------------
# To enable real AI features, get your API key from:
# https://aistudio.google.com/app/apikey
#
# Without an API key, RecruitPro runs in fallback mode with intelligent
# heuristics - the app will still work, just without advanced AI features.
#
# UNCOMMENT AND SET YOUR API KEY TO ENABLE REAL AI:
# RECRUITPRO_GEMINI_API_KEY=your-gemini-api-key-here

# Monitoring - OPTIONAL for local use
# -----------------------------------------------------------------------------
# Sentry error tracking is optional for local deployment
# RECRUITPRO_SENTRY_DSN=
RECRUITPRO_SENTRY_ENVIRONMENT=development
RECRUITPRO_SENTRY_TRACES_SAMPLE_RATE=0.0
